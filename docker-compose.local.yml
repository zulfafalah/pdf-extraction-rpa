volumes:
  rpa_project_local_postgres_data: {}
  rpa_project_local_postgres_data_backups: {}
  rpa_project_local_redis_data: {}

services:
  django: &django
    build:
      context: .
      dockerfile: ./compose/local/django/Dockerfile
    image: rpa_project_local_django
    container_name: rpa_project_local_django
    depends_on:
      - postgres
      - redis
    volumes:
      - /app/.venv
      - .:/app:z
    env_file:
      - ./.envs/.local/.django
      - ./.envs/.local/.postgres
    ports:
      - '8010:8000'
    command: /start

  postgres:
    build:
      context: .
      dockerfile: ./compose/production/postgres/Dockerfile
    image: rpa_project_production_postgres
    container_name: rpa_project_local_postgres
    volumes:
      - rpa_project_local_postgres_data:/var/lib/postgresql/18/docker
      - rpa_project_local_postgres_data_backups:/backups
    env_file:
      - ./.envs/.local/.postgres

  redis:
    image: docker.io/redis:8.4
    container_name: rpa_project_local_redis
    volumes:
      - rpa_project_local_redis_data:/data

  celeryworker:
    <<: *django
    image: rpa_project_local_celeryworker
    container_name: rpa_project_local_celeryworker
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celeryworker

  celerybeat:
    <<: *django
    image: rpa_project_local_celerybeat
    container_name: rpa_project_local_celerybeat
    depends_on:
      - redis
      - postgres
    ports: []
    command: /start-celerybeat

  flower:
    <<: *django
    image: rpa_project_local_flower
    container_name: rpa_project_local_flower
    ports:
      - '5555:5555'
    command: /start-flower
